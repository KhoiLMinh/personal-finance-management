# Bảng Kiểm Thử Chức Năng (Test Cases)

Tài liệu này chứa các kịch bản kiểm thử (Test Cases) cho các module chính của hệ thống. Mỗi Test Case được thiết kế với 9 trường thông tin cơ bản nhằm đảm bảo tính tái lập và truy vết dễ dàng.

---

## 1. Module Xác thực và Người dùng (User & Auth)

| Mã TC       | Chức năng / UC     | Mục tiêu                                              | Tiền điều kiện                               | Dữ liệu vào                                                           | Các bước                                     | Kết quả mong đợi                                                                    | Kết quả thực tế           | Trạng thái |
| ----------- | ------------------ | ----------------------------------------------------- | -------------------------------------------- | --------------------------------------------------------------------- | -------------------------------------------- | ----------------------------------------------------------------------------------- | ------------------------- | ---------- |
| TC-AUTH-001 | Đăng ký tài khoản  | Kiểm tra luồng đăng ký hợp lệ (Happy Path).           | Không có                                     | `username: "newuser"`, `email: "new@email.com"`, `password: "123456"` | Gọi hàm `register()` với request hợp lệ.     | HTTP 201; Lưu User vào DB và gọi `cloneAdminCategoriesForNewUser`.                  | Khớp với kết quả mong đợi | Pass       |
| TC-AUTH-002 | Đăng ký tài khoản  | Chặn đăng ký khi Username đã tồn tại.                 | Đã tồn tại user `testuser` trong DB.         | `username: "testuser"`                                                | Gọi hàm `register()` với username trùng.     | HTTP 400; Ném `RuntimeException "Tên đăng nhập đã tồn tại!"`. Không gọi `save`.     | Khớp với kết quả mong đợi | Pass       |
| TC-AUTH-003 | Đăng nhập hệ thống | Kiểm tra đăng nhập thành công với thông tin đúng.     | User `testuser` (pass: `123456`) đã tồn tại. | `username: "testuser"`, `password: "123456"`                          | Gọi hàm `login()` với thông tin đúng.        | HTTP 200; Trả về `AuthResponse` chứa JWT Token và `UserDTO`.                        | Khớp với kết quả mong đợi | Pass       |
| TC-AUTH-004 | Đăng nhập hệ thống | Chặn đăng nhập khi nhập sai mật khẩu.                 | User `testuser` đã tồn tại.                  | `username: "testuser"`, `password: "wrong_password"`                  | Gọi hàm `login()` với password sai.          | HTTP 400; Ném `RuntimeException "Sai tên đăng nhập hoặc mật khẩu!"`.                | Khớp với kết quả mong đợi | Pass       |
| TC-AUTH-005 | Đổi mật khẩu       | Đổi mật khẩu thành công khi đúng mật khẩu cũ.         | User có ID = `1L` tồn tại.                   | `oldPassword: "old_password"`, `newPassword: "new_password"`          | Gọi hàm `changePassword()` với dữ liệu đúng. | HTTP 200; Mật khẩu mới được mã hóa và gọi `userRepository.save()`.                  | Khớp với kết quả mong đợi | Pass       |
| TC-USER-001 | Xem hồ sơ cá nhân  | Trả về thông tin `UserDTO` khi đúng ID.               | User tồn tại trong DB.                       | `userId: 1L`                                                          | Gọi hàm `getUserById()` với ID hợp lệ.       | HTTP 200; Trả về `UserDTO` khớp thông tin.                                          | Khớp với kết quả mong đợi | Pass       |
| TC-USER-002 | Xem hồ sơ cá nhân  | Xử lý lỗi khi ID người dùng không tồn tại.            | User không tồn tại.                          | `userId: 99L`                                                         | Gọi hàm `getUserById()` với ID ảo.           | HTTP 400; Ném `RuntimeException "Không tìm thấy người dùng!"`.                      | Khớp với kết quả mong đợi | Pass       |
| TC-USER-003 | Cập nhật hồ sơ     | Cập nhật tên và tải ảnh lên Cloudinary.               | User tồn tại trong DB.                       | `fullName: "New Name"`, `avatar: MockMultipartFile`                   | Gọi hàm `updateUser()` đính kèm file ảnh.    | HTTP 200; Gọi Cloudinary upload, lưu URL mới vào DB.                                | Khớp với kết quả mong đợi | Pass       |
| TC-USER-004 | Cập nhật hồ sơ     | Cập nhật tên hiển thị mà không tải ảnh.               | User tồn tại trong DB.                       | `fullName: "New Name Only"`, `avatar: null`                           | Gọi hàm `updateUser()` không có file ảnh.    | HTTP 200; Cập nhật tên. Hàm Cloudinary KHÔNG được gọi.                              | Khớp với kết quả mong đợi | Pass       |
| TC-USER-005 | Quản lý người dùng | Admin thay đổi trạng thái (Khóa/Mở) tài khoản.        | User có `userCode` hợp lệ.                   | `userCode: "mock-uuid-1234"`                                          | Gọi hàm `toggleUserStatus()`.                | HTTP 200; Trạng thái `isActive` bị đảo ngược và lưu DB.                             | Khớp với kết quả mong đợi | Pass       |
| TC-USER-006 | Quản lý người dùng | Chặn Admin khóa tài khoản Quản trị viên gốc (ID = 1). | Tồn tại Root Admin (ID = 1).                 | `userCode: "root-uuid"`                                               | Gọi hàm `toggleUserStatus()` với ID = 1.     | HTTP 403/400; Ném `RuntimeException "Không thể khóa tài khoản Quản trị viên gốc!"`. | Khớp với kết quả mong đợi | Pass       |

---

## 2. Module Quản lý Danh mục (Category)

| Mã TC       | Chức năng / UC | Mục tiêu                                          | Tiền điều kiện                           | Dữ liệu đầu vào                                  | Các bước                                             | Kết quả mong đợi                                                                  | Kết quả thực tế           | Trạng thái |
| ----------- | -------------- | ------------------------------------------------- | ---------------------------------------- | ------------------------------------------------ | ---------------------------------------------------- | --------------------------------------------------------------------------------- | ------------------------- | ---------- |
| TC-CATE-001 | Tạo danh mục   | Kiểm tra luồng tạo danh mục mới thành công.       | User tồn tại trong DB.                   | `name: "Lương"`, `type: "INCOME"`                | Gọi hàm `createCategory()` với body hợp lệ.          | HTTP 201; Lưu thành công vào DB, trả về DTO.                                      | Khớp với kết quả mong đợi | Pass       |
| TC-CATE-002 | Tạo danh mục   | Từ chối tạo danh mục con khác loại với cha.       | Đã có danh mục cha (`EXPENSE`).          | `type: "INCOME"`, `parentId: ID của cha`         | Gọi hàm `createCategory()` với `parentId` lệch loại. | HTTP 400; Ném `RuntimeException "Danh mục con phải cùng loại với danh mục cha!"`. | Khớp với kết quả mong đợi | Pass       |
| TC-CATE-003 | Xóa danh mục   | Xóa vật lý danh mục chưa phát sinh giao dịch.     | Danh mục không chứa giao dịch/ngân sách. | `categoryId: 10L`, `userId: 1L`                  | Gọi hàm `deleteCategory()`.                          | HTTP 204; Hàm `delete()` của CSDL được gọi.                                       | Khớp với kết quả mong đợi | Pass       |
| TC-CATE-004 | Xóa danh mục   | Từ chối xóa khi danh mục đã có giao dịch.         | Danh mục chứa Transaction liên quan.     | `categoryId: 10L`, `userId: 1L`                  | Gọi hàm `deleteCategory()`.                          | HTTP 400; Ném `RuntimeException "Danh mục đã có giao dịch..."`.                   | Khớp với kết quả mong đợi | Pass       |
| TC-CATE-005 | Ẩn danh mục    | Đổi trạng thái `isHidden` (Soft Delete).          | Danh mục tồn tại.                        | `categoryId: 10L`, `userId: 1L`                  | Gọi hàm `hideCategory()`.                            | HTTP 204; Cờ `isHidden` đổi thành `true`, gọi `save()`.                           | Khớp với kết quả mong đợi | Pass       |
| TC-CATE-006 | Thêm Rule      | Thiết lập từ khóa nhận diện tự động cho danh mục. | Danh mục tồn tại.                        | `keyword: "Highlands"`, `priority: 11`           | Gọi hàm `addRule()`.                                 | HTTP 201; Rule được lưu vào DB và trả về DTO.                                     | Khớp với kết quả mong đợi | Pass       |
| TC-CATE-007 | Xóa Rule       | Ngăn chặn người dùng xóa Rule của danh mục khác.  | Rule thuộc về User khác.                 | `categoryId: 10L`, `userId: 1L`, `hackerId: 99L` | Gọi hàm `deleteRule()` với ID không khớp.            | HTTP 400; Ném `RuntimeException "Không tìm thấy danh mục!"`.                      | Khớp với kết quả mong đợi | Pass       |

---

## 3. Module Quản lý Ví (Wallet)

| Mã TC       | Chức năng / UC | Mục tiêu                                          | Tiền điều kiện                               | Dữ liệu đầu vào                                   | Các bước                                     | Kết quả mong đợi                                                                                      | Kết quả thực tế           | Trạng thái |
| ----------- | -------------- | ------------------------------------------------- | -------------------------------------------- | ------------------------------------------------- | -------------------------------------------- | ----------------------------------------------------------------------------------------------------- | ------------------------- | ---------- |
| TC-WALL-001 | Tạo Ví         | Khởi tạo ví mới với số dư ban đầu hợp lệ.         | User tồn tại.                                | `name: "Ví Tiết Kiệm"`, `balance: 100000`         | Gọi hàm `createWallet()`.                    | HTTP 201; Wallet được tạo và trả về DTO.                                                              | Khớp với kết quả mong đợi | Pass       |
| TC-WALL-002 | Tạo Ví         | Báo lỗi khi ID người dùng không tồn tại.          | User không tồn tại trong DB.                 | `userId: 99L`                                     | Gọi hàm `createWallet()` với ID ảo.          | HTTP 400; Ném `RuntimeException "Không tìm thấy người dùng!"`.                                        | Khớp với kết quả mong đợi | Pass       |
| TC-WALL-003 | Sửa Ví         | Chủ sở hữu cập nhật thông tin ví thành công.      | Ví thuộc sở hữu của User (`requesterId=1L`). | `walletId: 10L`, `name: "Ví Mới"`                 | Gọi hàm `updateWallet()`.                    | HTTP 200; Tên mới được cập nhật vào DB.                                                               | Khớp với kết quả mong đợi | Pass       |
| TC-WALL-004 | Sửa Ví         | Ngăn chặn sửa ví nếu không có quyền sở hữu.       | Ví thuộc User A, User B thao tác.            | `walletId: 10L`, `notOwnerId: 2L`                 | Gọi hàm `updateWallet()` bởi User B.         | HTTP 403; Ném `AccessDeniedException "Bạn không có quyền thao tác trên ví này!"`.                     | Khớp với kết quả mong đợi | Pass       |
| TC-WALL-005 | Xóa Ví         | Chủ sở hữu xóa hoàn toàn dữ liệu ví.              | User là Owner của ví.                        | `walletId: 10L`, `requesterId: 1L`                | Gọi hàm `deleteWallet()`.                    | HTTP 204; Thực thi lệnh `delete()` trên DB.                                                           | Khớp với kết quả mong đợi | Pass       |
| TC-WALL-006 | Chia sẻ Ví     | Chia sẻ cho tài khoản cùng nhóm Gia đình.         | Cùng chung Family, target user tồn tại.      | `email: "friend@example.com"`, `permission: VIEW` | Gọi hàm `shareWallet()`.                     | HTTP 200; Lưu `WalletMember`, gọi `emailService` gửi thông báo.                                       | Khớp với kết quả mong đợi | Pass       |
| TC-WALL-007 | Chia sẻ Ví     | Ràng buộc: Chặn tự chia sẻ quyền cho chính mình.  | Email truyền vào trùng email Owner.          | `email: "owner_user@example.com"`                 | Gọi hàm `shareWallet()` mời chính mình.      | HTTP 400; Ném `RuntimeException "Không thể mời chính chủ sở hữu vào ví!"`.                            | Khớp với kết quả mong đợi | Pass       |
| TC-WALL-008 | Chia sẻ Ví     | Ràng buộc: Chặn chia sẻ cho tài khoản ngoài nhóm. | Email thuộc Family khác.                     | `email: "stranger@example.com"`                   | Gọi hàm `shareWallet()` với user ngoài nhóm. | HTTP 400; Ném `RuntimeException "Chỉ có thể chia sẻ ví cho các thành viên trong cùng một Gia đình!"`. | Khớp với kết quả mong đợi | Pass       |

---

## 4. Module Quản lý Giao dịch (Transaction)

| Mã TC       | Chức năng / UC | Mục tiêu                                                | Tiền điều kiện                                      | Dữ liệu đầu vào                        | Các bước                       | Kết quả mong đợi                                                                        | Kết quả thực tế           | Trạng thái |
| ----------- | -------------- | ------------------------------------------------------- | --------------------------------------------------- | -------------------------------------- | ------------------------------ | --------------------------------------------------------------------------------------- | ------------------------- | ---------- |
| TC-TRAN-001 | Thêm GD        | Thêm GD CHI và trừ số dư Ví tương ứng.                  | User có quyền EDIT trên Ví.                         | `amount: 50000`, `type: EXPENSE`       | Gọi hàm `createTransaction()`. | HTTP 201; Lưu Transaction, gọi `updateBalance()` trừ 50.000.                            | Khớp với kết quả mong đợi | Pass       |
| TC-TRAN-002 | Thêm GD        | Chặn thêm giao dịch khi chỉ có quyền VIEW trên ví.      | User chỉ có quyền VIEW (`hasEditPermission=false`). | Payload tạo giao dịch hợp lệ.          | Gọi hàm `createTransaction()`. | HTTP 403; Ném `AccessDeniedException "Bạn không có quyền thêm giao dịch vào ví này!"`.  | Khớp với kết quả mong đợi | Pass       |
| TC-TRAN-003 | Xóa GD         | Xóa GD CHI thành công và hoàn trả số dư Ví gốc.         | Giao dịch tồn tại, User có quyền EDIT.              | `transactionId: 100L`, `userId: 10L`   | Gọi hàm `deleteTransaction()`. | HTTP 204; Xóa Transaction, gọi `updateBalance()` cộng lại 50.000.                       | Khớp với kết quả mong đợi | Pass       |
| TC-TRAN-004 | Xóa GD         | Ngăn User xóa giao dịch thuộc quyền quản lý người khác. | Giao dịch không thuộc sở hữu/không access được.     | `transactionId: 100L`, `hackerId: 99L` | Gọi hàm `deleteTransaction()`. | HTTP 400; Ném `RuntimeException "Không tìm thấy giao dịch hoặc truy cập trái phép!"`.   | Khớp với kết quả mong đợi | Pass       |
| TC-TRAN-005 | Sửa GD         | Cập nhật số tiền (CHI 50k -> 70k), bù trừ số dư Ví.     | User có quyền EDIT, GD tồn tại.                     | `amount: 70000`, `type: EXPENSE`       | Gọi hàm `updateTransaction()`. | HTTP 200; Lưu thay đổi, gọi `updateBalance()` trừ tiếp 20.000.                          | Khớp với kết quả mong đợi | Pass       |
| TC-TRAN-006 | Sửa GD         | Đổi loại (CHI 50k -> THU 100k), bù trừ số dư Ví.        | User có quyền EDIT, GD tồn tại.                     | `amount: 100000`, `type: INCOME`       | Gọi hàm `updateTransaction()`. | HTTP 200; Đổi loại, gọi `updateBalance()` cộng bù 150.000.                              | Khớp với kết quả mong đợi | Pass       |
| TC-TRAN-007 | Sửa GD         | Chặn sửa giao dịch khi chỉ có quyền VIEW.               | User chỉ có quyền VIEW trên ví chứa giao dịch.      | Payload cập nhật giao dịch.            | Gọi hàm `updateTransaction()`. | HTTP 403; Ném `AccessDeniedException "Bạn không có quyền sửa giao dịch trong ví này!"`. | Khớp với kết quả mong đợi | Pass       |
| TC-TRAN-008 | Xóa GD         | Chặn xóa giao dịch khi chỉ có quyền VIEW.               | User chỉ có quyền VIEW trên ví chứa giao dịch.      | `transactionId: 100L`, `userId: 11L`   | Gọi hàm `deleteTransaction()`. | HTTP 403; Ném `AccessDeniedException "Bạn không có quyền xóa giao dịch trong ví này!"`. | Khớp với kết quả mong đợi | Pass       |
| TC-TRAN-009 | Thêm GD        | Tự động gọi kiểm tra Ngân sách khi phát sinh khoản CHI. | Giao dịch loại EXPENSE hợp lệ.                      | `type: EXPENSE`                        | Gọi hàm `createTransaction()`. | Giao dịch lưu thành công, tự động trigger `budgetService.checkAndAlertBudget()`.        | Khớp với kết quả mong đợi | Pass       |

---

## 5. Module Quản lý Ngân sách (Budget)

| Mã TC       | Chức năng / UC | Mục tiêu                                          | Tiền điều kiện                               | Dữ liệu đầu vào                              | Các bước                         | Kết quả mong đợi                                                                   | Kết quả thực tế           | Trạng thái |
| ----------- | -------------- | ------------------------------------------------- | -------------------------------------------- | -------------------------------------------- | -------------------------------- | ---------------------------------------------------------------------------------- | ------------------------- | ---------- |
| TC-BUDG-001 | Tạo NS         | Khởi tạo ngân sách tháng thành công.              | Danh mục tồn tại, chưa trùng kỳ.             | `month: 8`, `year: 2026`, `limit: 5000000`   | Gọi hàm `createBudget()`.        | HTTP 201; Lưu Budget vào DB, trả về DTO.                                           | Khớp với kết quả mong đợi | Pass       |
| TC-BUDG-002 | Tạo NS         | Chặn tạo trùng lặp ngân sách cùng danh mục và kỳ. | Đã tồn tại Budget cho tháng/năm/danh mục đó. | Thông số y hệt `TC-BUDG-001`                 | Gọi hàm `createBudget()` lần 2.  | HTTP 400; Ném `RuntimeException "Bạn đã thiết lập ngân sách cho danh mục này..."`. | Khớp với kết quả mong đợi | Pass       |
| TC-BUDG-003 | Xem NS         | Truy xuất danh sách ngân sách của người dùng.     | Đã có dữ liệu ngân sách trong DB.            | `userId: 1L`, `pageable: unpaged`            | Gọi hàm `getBudgets()`.          | HTTP 200; Trả về Page chứa `BudgetDTO`.                                            | Khớp với kết quả mong đợi | Pass       |
| TC-BUDG-004 | Sửa NS         | Cập nhật hạn mức và lưu log lịch sử đối soát.     | Ngân sách tồn tại trong DB.                  | `limitAmount: 6000000`, `warningPercent: 90` | Gọi hàm `updateBudget()`.        | HTTP 200; Cập nhật DB, `budgetHistoryRepository` ghi log.                          | Khớp với kết quả mong đợi | Pass       |
| TC-BUDG-005 | Xóa NS         | Xóa hoàn toàn bản ghi ngân sách.                  | Ngân sách tồn tại, đúng chủ sở hữu.          | `budgetId: 100L`, `userId: 1L`               | Gọi hàm `deleteBudget()`.        | HTTP 204; Thực thi lệnh `delete()` thành công.                                     | Khớp với kết quả mong đợi | Pass       |
| TC-BUDG-006 | Cảnh báo       | Vượt 100% ngân sách: Đổi Status, bắn Alert mức 1. | Ngân sách 5M ACTIVE.                         | Tổng chi thực tế = 5.5M                      | Gọi hàm `checkAndAlertBudget()`. | Status đổi `EXCEED`, tạo Noti (Priority 1), gửi Email Khẩn.                        | Khớp với kết quả mong đợi | Pass       |
| TC-BUDG-007 | Cảnh báo       | Chạm ngưỡng (80%): Đổi cờ, bắn Alert mức 2.       | Ngân sách 5M, ngưỡng 80%.                    | Tổng chi thực tế = 4.5M                      | Gọi hàm `checkAndAlertBudget()`. | Cờ `warningSent=true`, tạo Noti (Priority 2), gửi Email.                           | Khớp với kết quả mong đợi | Pass       |
| TC-BUDG-008 | Cảnh báo       | Dưới ngưỡng: Không báo động giả mạo.              | Ngân sách 5M, ngưỡng 80%.                    | Tổng chi thực tế = 2M                        | Gọi hàm `checkAndAlertBudget()`. | Cờ `warningSent=false`, KHÔNG sinh Noti, KHÔNG gửi Email.                          | Khớp với kết quả mong đợi | Pass       |

---

## 6. Module Quản lý Gia đình (Family)

| Mã TC       | Chức năng / UC | Mục tiêu                                       | Tiền điều kiện                                | Dữ liệu đầu vào                   | Các bước                          | Kết quả mong đợi                                                        | Kết quả thực tế           | Trạng thái |
| ----------- | -------------- | ---------------------------------------------- | --------------------------------------------- | --------------------------------- | --------------------------------- | ----------------------------------------------------------------------- | ------------------------- | ---------- |
| TC-FAMI-001 | Tạo nhóm       | Khởi tạo nhóm và tự gán quyền Chủ (MANAGER).   | User chưa thuộc gia đình nào.                 | `name: "Gia đình mới"`            | Gọi hàm `createFamily()`.         | HTTP 201; Lưu Family, FamilyMember, sinh mã mời UUID.                   | Khớp với kết quả mong đợi | Pass       |
| TC-FAMI-002 | Tạo nhóm       | Chặn 1 User tham gia/tạo nhiều gia đình.       | User đã là thành viên của gia đình khác.      | `name: "Nhà thứ 2"`               | Gọi hàm `createFamily()`.         | HTTP 400; Ném `RuntimeException "Bạn đã thuộc một gia đình khác..."`.   | Khớp với kết quả mong đợi | Pass       |
| TC-FAMI-003 | Vào nhóm       | Khớp mã mời hợp lệ, vào nhóm thành công.       | Mã mời `ABC123XY` tồn tại, User chưa có nhóm. | `inviteCode: "ABC123XY"`          | Gọi hàm `joinFamily()`.           | HTTP 200; Thêm vào nhóm với vai trò MEMBER.                             | Khớp với kết quả mong đợi | Pass       |
| TC-FAMI-004 | Vào nhóm       | Chặn tham gia khi nhập sai mã mời.             | Mã mời không tồn tại trong hệ thống.          | `inviteCode: "SAICODE"`           | Gọi hàm `joinFamily()`.           | HTTP 400; Ném `RuntimeException "Mã mời không hợp lệ!"`.                | Khớp với kết quả mong đợi | Pass       |
| TC-FAMI-005 | Rời nhóm       | MEMBER tự rời đi, thu hồi quyền chia sẻ ví.    | User đang là thành viên (MEMBER).             | `userId: 2L`                      | Gọi hàm `leaveFamily()`.          | HTTP 200; Thu hồi quyền chia sẻ ví, xóa FamilyMember.                   | Khớp với kết quả mong đợi | Pass       |
| TC-FAMI-006 | Rời nhóm       | Chặn OWNER tự ý rời nhóm (phải dùng xóa nhóm). | User đang là Chủ gia đình (OWNER).            | `userId: 1L`                      | Gọi hàm `leaveFamily()`.          | HTTP 400; Ném `RuntimeException "Chủ gia đình không thể tự rời đi..."`. | Khớp với kết quả mong đợi | Pass       |
| TC-FAMI-007 | Xóa nhóm       | OWNER xóa hoàn toàn nhóm, thu hồi Ví chia sẻ.  | User là Chủ gia đình (OWNER).                 | `familyId: 10L`, `userId: 1L`     | Gọi hàm `deleteFamily()`.         | HTTP 204; Xóa Family, gọi `revokeAllSharingForFamily()`.                | Khớp với kết quả mong đợi | Pass       |
| TC-FAMI-008 | Xóa nhóm       | MEMBER không có quyền xóa nhóm.                | User chỉ là thành viên (MEMBER).              | `familyId: 10L`, `userId: 2L`     | Gọi hàm `deleteFamily()`.         | HTTP 403; Ném `AccessDeniedException "Chủ gia đình mới có quyền..."`.   | Khớp với kết quả mong đợi | Pass       |
| TC-FAMI-009 | Xem nhóm       | Lấy danh sách thành viên trong nhóm.           | User thuộc nhóm, gia đình tồn tại.            | `familyId: 10L`, `userId: 2L`     | Gọi hàm `getMembers()`.           | HTTP 200; Trả về Page chứa `FamilyMemberDTO`.                           | Khớp với kết quả mong đợi | Pass       |
| TC-FAMI-010 | Đổi quyền      | Nâng cấp quyền thành viên (MEMBER -> MANAGER). | Tồn tại Owner và Member trong nhóm.           | `role: MANAGER`, `memberId: 100L` | Gọi hàm `updateMemberRole()`.     | HTTP 200; Cập nhật vai trò mới vào DB.                                  | Khớp với kết quả mong đợi | Pass       |
| TC-FAMI-011 | Đổi mã         | Cấp mới mã mời (Regenerate Invite Code).       | User là Chủ gia đình (OWNER).                 | `familyId: 10L`, `userId: 1L`     | Gọi hàm `regenerateInviteCode()`. | HTTP 200; Sinh mã mới, lưu DB và trả về chuỗi UUID.                     | Khớp với kết quả mong đợi | Pass       |

---

## 7. Module Mục tiêu tiết kiệm (Saving Goal)

| Mã TC       | Chức năng / UC | Mục tiêu                                             | Tiền điều kiện                                    | Dữ liệu đầu vào                      | Các bước                      | Kết quả mong đợi                                                             | Kết quả thực tế           | Trạng thái |
| ----------- | -------------- | ---------------------------------------------------- | ------------------------------------------------- | ------------------------------------ | ----------------------------- | ---------------------------------------------------------------------------- | ------------------------- | ---------- |
| TC-SAVE-001 | Tạo Quỹ        | Khởi tạo thành công Mục tiêu tiết kiệm mới.          | User tồn tại.                                     | `Target: 20M`, `Title: "Mua Laptop"` | Gọi hàm `createSavingGoal()`. | HTTP 201; Lưu DB, trạng thái ban đầu `IN_PROGRESS`.                          | Khớp với kết quả mong đợi | Pass       |
| TC-SAVE-002 | Sửa Quỹ        | Tự động chuyển `COMPLETE` nếu hạ mức đích.           | Mục tiêu có `current` = 5M.                       | `targetAmount` mới = 4M              | Gọi hàm `updateSavingGoal()`. | HTTP 200; Cập nhật DB, trạng thái tự đổi thành `COMPLETE`.                   | Khớp với kết quả mong đợi | Pass       |
| TC-SAVE-003 | Nạp tiền       | Trích tiền từ Ví, cộng vào Quỹ và lưu Giao dịch.     | Ví đủ số dư, User có quyền EDIT Ví.               | `amount: 3M`, `walletId: 100L`       | Gọi hàm `addFunds()`.         | HTTP 200; Số dư Ví giảm, quỹ tăng, tạo giao dịch chuyển tiền.                | Khớp với kết quả mong đợi | Pass       |
| TC-SAVE-004 | Nạp tiền       | Chặn nạp tiền nếu Ví thanh toán không đủ số dư.      | Ví chỉ còn 15M.                                   | `amount: 20M`, `walletId: 100L`      | Gọi hàm `addFunds()`.         | HTTP 400; Ném `RuntimeException "Số dư trong ví không đủ..."`.               | Khớp với kết quả mong đợi | Pass       |
| TC-SAVE-005 | Nạp tiền       | Chặn trích tiền nếu chỉ có quyền VIEW trên Ví.       | User chỉ có quyền VIEW trên ví (`hasEdit=false`). | `amount: 1M`, `walletId: 100L`       | Gọi hàm `addFunds()`.         | HTTP 403; Ném `RuntimeException "Bạn không có quyền trích tiền từ ví này!"`. | Khớp với kết quả mong đợi | Pass       |
| TC-SAVE-006 | Nạp tiền       | Chặn nạp tiền khi Mục tiêu đã `COMPLETE`.            | Mục tiêu đã hoàn thành trước đó.                  | `amount: 1M`, `walletId: 100L`       | Gọi hàm `addFunds()`.         | HTTP 400; Ném `RuntimeException "Mục tiêu này đã hoàn thành..."`.            | Khớp với kết quả mong đợi | Pass       |
| TC-SAVE-007 | Nạp tiền       | Tự động hoàn thành (`COMPLETE`) khi nạp đủ mức đích. | Mục tiêu gần đạt hạn mức.                         | `amount: 15M` (Đạt target 20M)       | Gọi hàm `addFunds()`.         | HTTP 200; Tiền cộng đủ, trạng thái tự đổi sang `COMPLETE`.                   | Khớp với kết quả mong đợi | Pass       |

---

## 8. Module Quản lý Nhập dữ liệu (Import Batch)

| Mã TC      | Chức năng / UC | Mục tiêu                                               | Tiền điều kiện                                | Dữ liệu đầu vào                         | Các bước                    | Kết quả mong đợi                                                                        | Kết quả thực tế           | Trạng thái |
| ---------- | -------------- | ------------------------------------------------------ | --------------------------------------------- | --------------------------------------- | --------------------------- | --------------------------------------------------------------------------------------- | ------------------------- | ---------- |
| TC-IMP-001 | Trích xuất     | Đọc chuẩn xác các Headers từ cấu trúc file CSV.        | Không có                                      | File CSV: `"Ngày,Số tiền,Nội dung"`     | Gọi hàm `extractHeaders()`. | Trả về danh sách 3 cột đúng tên tiêu đề.                                                | Khớp với kết quả mong đợi | Pass       |
| TC-IMP-002 | Nạp CSV        | Chặn Import File vào Ví nếu chỉ có quyền VIEW.         | User chỉ có quyền VIEW trên ví đích.          | File CSV hợp lệ                         | Gọi hàm `importData()`.     | HTTP 403; Ném `AccessDeniedException "Bạn không có quyền import dữ liệu vào ví này!"`.  | Khớp với kết quả mong đợi | Pass       |
| TC-IMP-003 | Nạp CSV        | Import thành công, ưu tiên dùng Rule (Không gọi AI).   | Tồn tại Rule `"Highlands"` -> `"Ăn uống"`.    | File CSV chứa dòng `"Highlands Coffee"` | Gọi hàm `importData()`.     | Trả `successRows=1`. API `categorizeTransactionsBatch` KHÔNG chạy.                      | Khớp với kết quả mong đợi | Pass       |
| TC-IMP-004 | Khử trùng      | Bỏ qua các dòng giao dịch bị trùng (Deduplication).    | Giao dịch y hệt đã tồn tại trong CSDL.        | File CSV chứa dòng giao dịch cũ         | Gọi hàm `importData()`.     | Trả `duplicatedRows=1`. Hàm `saveAll()` KHÔNG thực thi.                                 | Khớp với kết quả mong đợi | Pass       |
| TC-IMP-005 | Nạp CSV        | Bắt lỗi định dạng số tiền/ngày và Rollback tiến trình. | File CSV chứa dữ liệu lỗi định dạng cột ngày. | Cột ngày = `"ABC"`                      | Gọi hàm `importData()`.     | Ném `IllegalArgumentException "Phát hiện dòng sai định dạng số tiền hoặc ngày tháng!"`. | Khớp với kết quả mong đợi | Pass       |

---

## 9. Module Quản lý Thông báo (Notification)

| Mã TC       | Chức năng / UC | Mục tiêu                                            | Tiền điều kiện                                | Dữ liệu đầu vào                        | Các bước                                      | Kết quả mong đợi                                                                      | Kết quả thực tế           | Trạng thái |
| ----------- | -------------- | --------------------------------------------------- | --------------------------------------------- | -------------------------------------- | --------------------------------------------- | ------------------------------------------------------------------------------------- | ------------------------- | ---------- |
| TC-NOTI-001 | Xem Noti       | Gọi đúng câu truy vấn ưu tiên Sắp xếp (Priority).   | Đã có thông báo trong DB.                     | `userId: 1L`, `pageable: unpaged`      | Gọi hàm `getMyNotifications()`.               | Gọi Repository `findMyPriorityNotifications()` thành công.                            | Khớp với kết quả mong đợi | Pass       |
| TC-NOTI-002 | Đọc 1 lần      | Đánh dấu 1 Thông báo là đã đọc (Read).              | Thông báo trạng thái `is_read = false`.       | `notificationId: 10L`, `userId: 1L`    | Gọi hàm `markAsRead()`.                       | Gọi Query `markAsRead()` cập nhật cờ thành công.                                      | Khớp với kết quả mong đợi | Pass       |
| TC-NOTI-003 | Đọc tất cả     | Đánh dấu Toàn bộ thông báo là đã đọc (Read All).    | User có nhiều thông báo chưa đọc.             | `userId: 1L`                           | Gọi hàm `markAllAsRead()`.                    | Gọi Query `markAllAsRead()` xử lý hàng loạt thành công.                               | Khớp với kết quả mong đợi | Pass       |
| TC-NOTI-004 | Xóa Noti       | Xóa thông báo của chính mình thành công.            | Thông báo thuộc về User đang thao tác.        | `notificationId: 10L`, `userId: 1L`    | Gọi hàm `deleteNotification()`.               | Lệnh `delete()` thực thi xóa bản ghi khỏi DB.                                         | Khớp với kết quả mong đợi | Pass       |
| TC-NOTI-005 | Xóa Noti       | Chặn xóa thông báo thuộc về User khác (IDOR).       | Thông báo thuộc về Nạn nhân, Hacker thao tác. | `notificationId: 10L`, `hackerId: 99L` | Gọi hàm `deleteNotification()` với ID hacker. | HTTP 400; Ném `RuntimeException "Không tìm thấy thông báo hoặc truy cập trái phép!"`. | Khớp với kết quả mong đợi | Pass       |
| TC-NOTI-006 | Tạo ngầm       | Tạo thông báo hệ thống với mức ưu tiên chính xác.   | User tồn tại trong DB.                        | `userId: 1L`, `priority: 2`            | Gọi hàm `createSystemNotification()`.         | Bản ghi Notification lưu đúng `Priority` tương ứng.                                   | Khớp với kết quả mong đợi | Pass       |
| TC-NOTI-007 | Tạo ngầm       | Xử lý lỗi khi tạo thông báo cho User không tồn tại. | User ID không có trong hệ thống.              | `userId: 99L`                          | Gọi hàm `createSystemNotification()`.         | HTTP 400; Ném `RuntimeException "Không tìm thấy người dùng để gửi thông báo!"`.       | Khớp với kết quả mong đợi | Pass       |

---

## 10. Module Hóa đơn định kỳ & Scheduler (Recurring Bill & Task Scheduler)

| Mã TC        | Chức năng / UC | Mục tiêu                                         | Tiền điều kiện                                | Dữ liệu đầu vào              | Các bước                                                   | Kết quả mong đợi                                                       | Kết quả thực tế           | Trạng thái |
| ------------ | -------------- | ------------------------------------------------ | --------------------------------------------- | ---------------------------- | ---------------------------------------------------------- | ---------------------------------------------------------------------- | ------------------------- | ---------- |
| TC-SCHED-001 | Quét HĐ        | Bắn cảnh báo mức 1 khi Hóa đơn đến hạn HÔM NAY.  | DB có hóa đơn đến hạn hôm nay, đúng giờ hẹn.  | `executionDay: Today`        | Hệ thống kích hoạt Cron Job `processRecurringBills()`.     | Tạo Noti (`Priority=1`), gửi Email khẩn, cập nhật `lastExecuted`.      | Khớp với kết quả mong đợi | Pass       |
| TC-SCHED-002 | Quét HĐ        | Bắn cảnh báo mức 2 khi Hóa đơn đến hạn NGÀY MAI. | DB có hóa đơn đến hạn ngày mai, đúng giờ hẹn. | `executionDay: Tomorrow`     | Hệ thống kích hoạt Cron Job `processRecurringBills()`.     | Tạo Noti (`Priority=2`) nhắc nhở trước 1 ngày, cập nhật `lastWarning`. | Khớp với kết quả mong đợi | Pass       |
| TC-SCHED-003 | Quét HĐ        | Bỏ qua nếu cấu hình giờ báo thức chưa tới.       | Hóa đơn có giờ thông báo ở tương lai.         | `notificationTime: Now + 1h` | Hệ thống kích hoạt Cron Job `processRecurringBills()`.     | Cron Job quét qua nhưng không xử lý (Do nothing).                      | Khớp với kết quả mong đợi | Pass       |
| TC-SCHED-004 | Quét Quỹ       | Bắn cảnh báo mức 1 khi Quỹ tiết kiệm ĐẾN HẠN.    | DB có Mục tiêu tiết kiệm đến hạn hôm nay.     | `deadline: Today`            | Hệ thống kích hoạt Cron Job `checkAndNotifySavingGoals()`. | Tạo Noti (`Priority=1`) báo Mục tiêu đáo hạn.                          | Khớp với kết quả mong đợi | Pass       |
| TC-SCHED-005 | Quét Quỹ       | Bắn cảnh báo mức 2 khi Quỹ ĐẾN HẠN TRONG 3 NGÀY. | DB có Mục tiêu hết hạn sau 3 ngày.            | `deadline: Today + 3 days`   | Hệ thống kích hoạt Cron Job `checkAndNotifySavingGoals()`. | Tạo Noti (`Priority=2`) báo nhắc nhở sớm trước 3 ngày.                 | Khớp với kết quả mong đợi | Pass       |

---

## 11. Module Báo cáo và Thống kê (Report)

| Mã TC       | Chức năng / UC | Mục tiêu                                            | Tiền điều kiện                                       | Dữ liệu đầu vào                       | Các bước                                             | Kết quả mong đợi                                                | Kết quả thực tế           | Trạng thái |
| ----------- | -------------- | --------------------------------------------------- | ---------------------------------------------------- | ------------------------------------- | ---------------------------------------------------- | --------------------------------------------------------------- | ------------------------- | ---------- |
| TC-REPO-001 | Thống kê       | Tính toán đúng tổng thu/chi/số dư và % tăng trưởng. | DB có sẵn các giao dịch mẫu qua các kỳ.              | `startDate`, `endDate` tháng hiện tại | Gọi hàm `getDashboardOverview()`.                    | Trả về DTO tính toán chính xác số liệu, biểu đồ xu hướng.       | Khớp với kết quả mong đợi | Pass       |
| TC-REPO-002 | Thống kê       | Nội suy an toàn chống lỗi Division by Zero.         | Dữ liệu giao dịch kỳ trước bằng null/trống.          | `startDate`, `endDate` hợp lệ         | Gọi hàm `getDashboardOverview()` với kỳ trước trống. | Xử lý an toàn, tự nội suy trả về `incomeChangePercent = 100.0`. | Khớp với kết quả mong đợi | Pass       |
| TC-REPO-003 | Xuất file      | Kết xuất mảng Byte của file báo cáo Excel.          | DB có dữ liệu giao dịch trong khoảng thời gian chọn. | `startDate`, `endDate`, `userId: 1L`  | Gọi hàm `exportTransactionsToExcel()`.               | Trả về mảng Byte (ByteArray) hợp lệ, không rỗng.                | Khớp với kết quả mong đợi | Pass       |
| TC-REPO-004 | Xuất file      | Kết xuất mảng Byte của file báo cáo PDF.            | DB có dữ liệu giao dịch trong khoảng thời gian chọn. | `startDate`, `endDate`, `userId: 1L`  | Gọi hàm `exportTransactionsToPdf()`.                 | Trả về mảng Byte (ByteArray) hợp lệ, không rỗng.                | Khớp với kết quả mong đợi | Pass       |

## 12. Module Sao lưu và Phục hồi dữ liệu (Backup & Restore)

| Mã TC       | Chức năng / UC   | Mục tiêu                                                                        | Tiền điều kiện                                   | Dữ liệu đầu vào                                 | Các bước                    | Kết quả mong đợi                                                     |
| ----------- | ---------------- | ------------------------------------------------------------------------------- | ------------------------------------------------ | ----------------------------------------------- | --------------------------- | -------------------------------------------------------------------- |
| TC-BACK-001 | Sao lưu dữ liệu  | Kết xuất toàn bộ dữ liệu tài chính (Ví, Giao dịch, Ngân sách, Quỹ) ra tệp JSON. | User tồn tại trong DB, có hoặc không có dữ liệu. | `userId: 1L`                                    | Gọi hàm `exportUserData()`. | Trả về mảng Byte chứa chuỗi JSON hợp lệ, có trường `exportDate`.     |
| TC-BACK-002 | Phục hồi dữ liệu | Đọc tệp JSON sao lưu hợp lệ và lưu khôi phục vào CSDL.                          | Tệp JSON đúng định dạng `BackupDataDTO`.         | `MultipartFile` JSON hợp lệ, chứa thông tin ví. | Gọi hàm `importUserData()`. | Phân tích JSON thành công, gọi lệnh `save()` để lưu Ví mới vào CSDL. |
| TC-BACK-003 | Phục hồi dữ liệu | Chặn thao tác khôi phục nếu tệp JSON bị hỏng, sai cấu trúc.                     | Tệp JSON bị lỗi cú pháp hoặc mã hóa sai.         | `MultipartFile` JSON hỏng (`invalid-json`).     | Gọi hàm `importUserData()`. | HTTP 400; Ném `RuntimeException "File sao lưu không hợp lệ..."`.     |
